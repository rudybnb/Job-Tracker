import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.socialcare.homes',
  appName: 'SocialCareHomes',
  webDir: 'dist/public'
};

export default config;
